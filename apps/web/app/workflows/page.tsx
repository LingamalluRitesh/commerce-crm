import { WorkflowView } from "@/components/modules/workflow-view";

export const metadata = {
  title: "Workflow Automation Studio — CommerceCRM",
};

export default function WorkflowsPage() {
  return <WorkflowView />;
}
