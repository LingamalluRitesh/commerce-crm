import { SupportView } from "@/components/modules/support-view";

export const metadata = {
  title: "Customer Support & SLAs — CommerceCRM",
};

export default function SupportPage() {
  return <SupportView />;
}
