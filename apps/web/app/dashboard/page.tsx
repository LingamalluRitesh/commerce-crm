import { DashboardView } from "@/components/modules/dashboard-view";

export const metadata = {
  title: "Executive Dashboard — CommerceCRM",
};

export default function DashboardPage() {
  return <DashboardView />;
}
