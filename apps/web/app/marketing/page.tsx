import { MarketingView } from "@/components/modules/marketing-view";

export const metadata = {
  title: "Marketing Automation — CommerceCRM",
};

export default function MarketingPage() {
  return <MarketingView />;
}
