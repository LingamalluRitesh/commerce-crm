import { CustomerView } from "@/components/modules/customer-view";

export const metadata = {
  title: "Customer 360 & Accounts — CommerceCRM",
};

export default function CustomersPage() {
  return <CustomerView />;
}
