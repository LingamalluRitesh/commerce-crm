import { InventoryView } from "@/components/modules/inventory-view";

export const metadata = {
  title: "Inventory & Warehouses — CommerceCRM",
};

export default function InventoryPage() {
  return <InventoryView />;
}
